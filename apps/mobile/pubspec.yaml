name: construction_field_app
description: Construction Management Field App — offline-first field evidence OS (RAYNS MVP + mid-market differentiators).
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  intl: ^0.20.2
  connectivity_plus: ^6.1.4
  shared_preferences: ^2.5.3

  # Wired in Phase 1 after Firebase project is created (flutterfire configure).
  # firebase_core: ^3.15.1
  # firebase_auth: ^5.6.2
  # cloud_firestore: ^5.6.11
  # firebase_storage: ^12.4.9
  # firebase_messaging: ^15.2.9
  # firebase_crashlytics: ^4.3.9
  # firebase_analytics: ^11.5.2
  # local_auth: ^2.3.0
  # geolocator: ^14.0.1
  # image_picker: ^1.1.2
  # drift: ^2.26.1
  # path_provider: ^2.1.5
  # path: ^1.9.1
  # workmanager: ^0.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  generate: true
